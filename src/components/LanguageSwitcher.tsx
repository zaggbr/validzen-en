import { useI18n, Locale } from "@/i18n/I18nContext";
import { useNavigate, useLocation } from "react-router-dom";

const LanguageSwitcher = () => {
  // Temporarily disabled — EN content not yet available
  return null;
};

export default LanguageSwitcher;
